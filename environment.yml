name: nutrigraph
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - pip:
      - streamlit>=1.28.0
      - pydantic>=2.0.0
      - pandas>=2.0.0
      - python-dotenv>=1.0.0
      - requests>=2.31.0
